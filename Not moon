local player = game.Players.LocalPlayer
local screenGui = Instance.new("ScreenGui", game.CoreGui)
screenGui.ResetOnSpawn = false

-- 1. واجهة البداية (START UI)
local startFrame = Instance.new("Frame", screenGui)
startFrame.Size = UDim2.new(0, 220, 0, 150)
startFrame.Position = UDim2.new(0.5, -110, 0.5, -75)
startFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Instance.new("UICorner", startFrame).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", startFrame).Color = Color3.fromRGB(0, 170, 255)

local logo = Instance.new("TextLabel", startFrame)
logo.Size = UDim2.new(1, 0, 0, 50)
logo.Text = "NX2 v1"
logo.TextColor3 = Color3.fromRGB(0, 170, 255)
logo.TextSize = 26
logo.Font = Enum.Font.SourceSansBold
logo.BackgroundTransparency = 1

local startBtn = Instance.new("TextButton", startFrame)
startBtn.Size = UDim2.new(0.85, 0, 0.35, 0)
startBtn.Position = UDim2.new(0.075, 0, 0.5, 0)
startBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
startBtn.Text = "START"
startBtn.TextColor3 = Color3.new(1, 1, 1)
startBtn.TextSize = 20
Instance.new("UICorner", startBtn).CornerRadius = UDim.new(0, 8)

-- 2. واجهة التحكم الرئيسية
local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Size = UDim2.new(0, 220, 0, 320)
mainFrame.Position = UDim2.new(0.5, -110, 0.5, -160)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
mainFrame.Visible = false
mainFrame.Active = true
mainFrame.Draggable = true
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", mainFrame).Color = Color3.fromRGB(0, 170, 255)

local mainLogo = Instance.new("TextLabel", mainFrame)
mainLogo.Size = UDim2.new(1, 0, 0, 40)
mainLogo.Text = "NX2 v1"
mainLogo.TextColor3 = Color3.fromRGB(0, 170, 255)
mainLogo.TextSize = 22
mainLogo.Font = Enum.Font.SourceSansBold
mainLogo.BackgroundTransparency = 1

local function createBtn(text, pos, color)
    local btn = Instance.new("TextButton", mainFrame)
    btn.Size = UDim2.new(0.85, 0, 0.12, 0)
    btn.Position = pos
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 14
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    return btn
end

-- الأزرار بالترتيب والألوان المطلوبة
local upBtn = createBtn("Gap Up", UDim2.new(0.075, 0, 0.18, 0), Color3.fromRGB(0, 100, 255)) -- أزرق
local downBtn = createBtn("Gap Down", UDim2.new(0.075, 0, 0.34, 0), Color3.fromRGB(0, 100, 255)) -- أزرق
local takeBtn = createBtn("Instant Take: OFF", UDim2.new(0.075, 0, 0.50, 0), Color3.fromRGB(120, 0, 0))
local zoomBtn = createBtn("ZOOM: OFF", UDim2.new(0.075, 0, 0.66, 0), Color3.fromRGB(120, 0, 0))
local discordBtn = createBtn("نسخ سيرفر ديسكورد", UDim2.new(0.075, 0, 0.82, 0), Color3.fromRGB(88, 101, 242))

-- 3. البرمجة والمنطق
startBtn.MouseButton1Click:Connect(function()
    startFrame.Visible = false
    mainFrame.Visible = true
end)

-- وظيفة الانزلاق للفتحات (Smooth Glide)
local function glide(direction)
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if root then
        local speed = char.Humanoid.WalkSpeed
        local targetCFrame = root.CFrame * CFrame.new(0, 0, direction * (speed * 0.4))
        
        -- نظام مشي تلقائي سريع جداً للوصول للفتحة
        local tweenService = game:GetService("TweenService")
        local info = TweenInfo.new(0.2, Enum.EasingStyle.Linear)
        tweenService:Create(root, info, {CFrame = targetCFrame}):Play()
    end
end

-- زر Gap Up
upBtn.MouseButton1Click:Connect(function()
    upBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- أخضر
    glide(-1)
    task.wait(0.5)
    upBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 255) -- رجوع أزرق
end)

-- زر Gap Down مع حماية البيت
local currentPos = 0
downBtn.MouseButton1Click:Connect(function()
    if currentPos > 0 or player.Character.HumanoidRootPart.Position.Z < -50 then
        downBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        glide(1)
        currentPos = currentPos - 1
        task.wait(0.5)
        downBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
    end
end)

-- أخذ الأغراض والزوم
local it = false
takeBtn.MouseButton1Click:Connect(function()
    it = not it
    takeBtn.Text = it and "Instant Take: ON" or "Instant Take: OFF"
    takeBtn.BackgroundColor3 = it and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(120, 0, 0)
end)

game:GetService("RunService").Heartbeat:Connect(function()
    if it then
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") then v.HoldDuration = 0 end
        end
    end
end)

zoomBtn.MouseButton1Click:Connect(function()
    local z = zoomBtn.Text == "ZOOM: OFF"
    player.CameraMaxZoomDistance = z and 500 or 128
    zoomBtn.Text = z and "ZOOM: ON" or "ZOOM: OFF"
    zoomBtn.BackgroundColor3 = z and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(120, 0, 0)
end)

discordBtn.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/syPpUv8Jf")
    discordBtn.Text = "تم النسخ!"
    task.wait(2)
    discordBtn.Text = "نسخ سيرفر ديسكورد"
end)