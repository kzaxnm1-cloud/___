-- 1. شاشة الترحيب (Hello) تملأ الشاشة لمدة ثانيتين-- 1. شاشة الترحيب (Hello) شفافة لمدة ثانيتين
local welcomeGui = Instance.new("ScreenGui", game.CoreGui)
local helloTxt = Instance.new("TextLabel", welcomeGui)
helloTxt.Size = UDim2.new(1, 0, 1, 0)
helloTxt.Text = "Hello"
helloTxt.TextColor3 = Color3.fromRGB(255, 255, 255)
helloTxt.TextSize = 100
helloTxt.BackgroundTransparency = 1 -- خلفية شفافة تماماً
helloTxt.Font = Enum.Font.SourceSansBold
helloTxt.TextStrokeTransparency = 0 -- حدود سوداء خفيفة للنص ليكون واضحاً

task.delay(2, function()
    welcomeGui:Destroy()
end)

-- 2. إعدادات اللاعب والنظام
local player = game.Players.LocalPlayer
local noclip = false
local runService = game:GetService("RunService")

-- وظيفة التعامل مع الموت (إعادة ضبط الخيار)
player.CharacterAdded:Connect(function(newChar)
    noclip = false -- إيقاف الاختراق عند الموت
    -- سيتم تحديث الزر في الأسفل عبر مراقبة المتغير
end)

-- حلقة التشغيل الدائمة للاختراق
runService.Stepped:Connect(function()
    if noclip and player.Character then
        for _, part in pairs(player.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end)

-- 3. واجهة NX2
local mainGui = Instance.new("ScreenGui", game.CoreGui)
local frame = Instance.new("Frame", mainGui)
frame.Size = UDim2.new(0, 220, 0, 160)
frame.Position = UDim2.new(0.5, -110, 0.5, -80)
frame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
frame.BorderSizePixel = 2
frame.Active = true
frame.Draggable = true

local nx2Title = Instance.new("TextLabel", frame)
nx2Title.Size = UDim2.new(1, 0, 0, 40)
nx2Title.Text = "NX2"
nx2Title.TextColor3 = Color3.fromRGB(255, 255, 0)
nx2Title.TextSize = 25
nx2Title.BackgroundTransparency = 1

local subTitle = Instance.new("TextLabel", frame)
subTitle.Position = UDim2.new(0, 0, 0.25, 0)
subTitle.Size = UDim2.new(1, 0, 0, 30)
subTitle.Text = "تفعيل اختراق"
subTitle.TextColor3 = Color3.fromRGB(255, 0, 0)
subTitle.TextSize = 18
subTitle.BackgroundTransparency = 1

local btn = Instance.new("TextButton", frame)
btn.Size = UDim2.new(0.8, 0,end)