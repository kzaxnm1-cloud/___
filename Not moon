local player = game.Players.LocalPlayer
local screenGui = Instance.new("ScreenGui", game.CoreGui)
screenGui.ResetOnSpawn = false

-- الواجهة الرئيسية
local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Size = UDim2.new(0, 220, 0, 320)
mainFrame.Position = UDim2.new(0.5, -110, 0.4, -160)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
mainFrame.Active = true
mainFrame.Draggable = true
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", mainFrame).Color = Color3.fromRGB(0, 170, 255)

local logo = Instance.new("TextLabel", mainFrame)
logo.Size = UDim2.new(1, 0, 0, 40)
logo.Text = "NX2 v1"
logo.TextColor3 = Color3.fromRGB(0, 170, 255)
logo.TextSize = 22
logo.Font = Enum.Font.SourceSansBold
logo.BackgroundTransparency = 1

local function createBtn(text, pos, color)
    local btn = Instance.new("TextButton", mainFrame)
    btn.Size = UDim2.new(0.85, 0, 0.12, 0)
    btn.Position = pos
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 14
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    return btn
end

local upBtn = createBtn("Gap Up", UDim2.new(0.075, 0, 0.18, 0), Color3.fromRGB(0, 100, 255))
local downBtn = createBtn("Gap Down", UDim2.new(0.075, 0, 0.34, 0), Color3.fromRGB(0, 100, 255))
local takeBtn = createBtn("Instant Take: OFF", UDim2.new(0.075, 0, 0.50, 0), Color3.fromRGB(120, 0, 0))
local zoomBtn = createBtn("ZOOM: OFF", UDim2.new(0.075, 0, 0.66, 0), Color3.fromRGB(120, 0, 0))
local discordBtn = createBtn("نسخ سيرفر ديسكورد", UDim2.new(0.075, 0, 0.82, 0), Color3.fromRGB(88, 101, 242))

-- [ نظام المشي التلقائي الصاروخي ] --
local function autoWalkToGap(direction)
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if root then
        -- الألوان: يقلب أخضر أول ما يبدأ يمشي
        local targetBtn = (direction == 1) and upBtn or downBtn
        local originalColor = Color3.fromRGB(0, 100, 255)
        targetBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)

        -- السرعة: نأخذ سرعة مشيك الحالية ونخليها "دفع"
        local speed = char.Humanoid.WalkSpeed
        local vel = Instance.new("BodyVelocity")
        vel.MaxForce = Vector3.new(100000, 0, 100000)
        -- يمشي سيدة للفتحة (على محور الماب) وبسرعة مضاعفة لسرعتك
        vel.Velocity = root.CFrame.LookVector * (speed * 2.5 * direction)
        vel.Parent = root

        -- مدة المشي (يوقف لما يوصل للفتحة اللي بعدها بالضبط)
        task.wait(0.4) 
        
        vel:Destroy()
        targetBtn.BackgroundColor3 = originalColor -- يرجع أزرق
    end
end

upBtn.MouseButton1Click:Connect(function()
    autoWalkToGap(1) -- يمشي من نفسه للفتحة اللي قدام
end)

downBtn.MouseButton1Click:Connect(function()
    -- حماية البيت: يرجعك بس لو لسه بعيد عن البداية
    local hrp = player.Character.HumanoidRootPart
    if hrp.Position.Z < -25 then
        autoWalkToGap(-1) -- يمشي من نفسه للفتحة اللي ورا
    else
        downBtn.Text = "أنت بالبيت!"
        task.wait(1)
        downBtn.Text = "Gap Down"
    end
end)

-- ميزات Instant Take و Zoom
local it = false
takeBtn.MouseButton1Click:Connect(function()
    it = not it
    takeBtn.Text = it and "Instant Take: ON" or "Instant Take: OFF"
    takeBtn.BackgroundColor3 = it and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(120, 0, 0)
end)

game:GetService("RunService").Heartbeat:Connect(function()
    if it then
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") then v.HoldDuration = 0 end
        end
    end
end)

zoomBtn.MouseButton1Click:Connect(function()
    local z = zoomBtn.Text == "ZOOM: OFF"
    player.CameraMaxZoomDistance = z and 500 or 128
    zoomBtn.Text = z and "ZOOM: ON" or "ZOOM: OFF"
    zoomBtn.BackgroundColor3 = z and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(120, 0, 0)
end)

discordBtn.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/syPpUv8Jf")
    discordBtn.Text = "تم النسخ!"
    task.wait(2)
    discordBtn.Text = "نسخ سيرفر ديسكورد"
end)